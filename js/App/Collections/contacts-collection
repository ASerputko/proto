define(
    ['jbone', 'lodash', 'backbone', 'Models/contact-model'],
    function ($, _, Backbone, contactModel) {
        var contacts;

        contacts = Backbone.Collection.extend({
            model: contactModel
        });

        return contacts;
    }
);
